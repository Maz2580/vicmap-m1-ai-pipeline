# Core runtime dependencies for vicmap-m1-ai-pipeline.

# --- Web framework ---
Flask>=2.3.3
Flask-CORS>=4.0.0

# --- HTTP / API clients ---
requests>=2.31.0

# --- Data processing ---
pandas>=2.1.0
geopandas>=0.14.0
shapely>=2.0.0

# --- Scheduling / orchestration ---
schedule>=1.2.0

# --- Database (TechnologyOne InfoProd SQL Server) ---
pyodbc>=5.0.0

# --- LLM clients ---
# `openai` covers OpenAI, OpenRouter, Groq, Ollama, and Together.ai — all of
# these expose an OpenAI-compatible Chat Completions API. v1+ is required for
# the modern `from openai import OpenAI` import path.
openai>=1.40.0

# --- Configuration ---
python-dotenv>=1.0.0

# =============================================================================
# Optional extras — uncomment the lines for the providers you actually use.
# =============================================================================
# Anthropic Claude support (LLM_PROVIDER=anthropic):
# anthropic>=0.40.0
